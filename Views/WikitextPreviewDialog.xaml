<ui:FluentWindow x:Class="MergeMansionWikiTools.Views.WikitextPreviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Preview" Height="820" Width="820"
        MinWidth="600" MinHeight="500"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True" WindowBackdropType="Mica">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar x:Name="titleBar" Grid.Row="0" Title="Preview" Height="36" />

        <Grid Grid.Row="1" Margin="24,10,24,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,12">
                <TextBlock x:Name="txtTitle" FontSize="18" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                <TextBlock x:Name="txtSubtitle" FontSize="12"
                           Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                           Margin="0,3,0,0" TextWrapping="Wrap" />
            </StackPanel>

            <!-- Preview -->
            <Border Grid.Row="1"
                    Background="{DynamicResource CardBackgroundFillColorSecondaryBrush}"
                    CornerRadius="4"
                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1">
                <TextBox x:Name="txtOutput"
                         Background="Transparent"
                         Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                         BorderThickness="0" IsReadOnly="True"
                         FontFamily="Consolas" FontSize="12"
                         AcceptsReturn="True" TextWrapping="NoWrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         Padding="12,10" />
            </Border>

            <!-- Bottom bar -->
            <Grid Grid.Row="2" Margin="0,12,0,0" MinHeight="50">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <ui:InfoBar x:Name="infoBar" Grid.Column="0" IsOpen="False"
                            IsClosable="True" VerticalAlignment="Stretch"
                            Margin="0,0,12,0" />

                <ui:Button Grid.Column="1" x:Name="btnPrimary" Visibility="Collapsed"
                           Icon="{ui:SymbolIcon CloudArrowUp24}"
                           Appearance="Primary" Click="BtnPrimary_Click"
                           VerticalAlignment="Stretch" Margin="0,1,8,2" />
                <ui:Button Grid.Column="2" x:Name="btnCopy"
                           Content="Copy to Clipboard" Icon="{ui:SymbolIcon Copy24}"
                           Appearance="Secondary" Click="BtnCopy_Click"
                           VerticalAlignment="Stretch" Margin="0,1,8,2" />
                <ui:Button Grid.Column="3" Content="Close"
                           Appearance="Secondary" Click="BtnClose_Click"
                           VerticalAlignment="Stretch" Margin="0,1,0,2" />
            </Grid>
        </Grid>
    </Grid>
</ui:FluentWindow>
