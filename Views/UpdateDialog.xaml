<ui:FluentWindow x:Class="MergeMansionWikiTools.Views.UpdateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Update Available"
        Width="640" Height="580"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterScreen"
        ExtendsContentIntoTitleBar="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />  <!-- TitleBar -->
            <RowDefinition Height="Auto" />  <!-- Header -->
            <RowDefinition Height="Auto" />  <!-- Version info -->
            <RowDefinition Height="*" />     <!-- Changelog -->
            <RowDefinition Height="Auto" />  <!-- Progress -->
            <RowDefinition Height="Auto" />  <!-- Buttons -->
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" ShowMinimize="False" ShowMaximize="False" ShowClose="True" />

        <!-- Header -->
        <StackPanel Grid.Row="1" Margin="24,0,24,16">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                <ui:SymbolIcon Symbol="ArrowDownload24" FontSize="24" Margin="0,0,10,0"
                               Foreground="{DynamicResource AccentFillColorDefaultBrush}" />
                <TextBlock x:Name="txtTitle" Text="A new version is available!"
                           FontSize="20" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            </StackPanel>
        </StackPanel>

        <!-- Version info -->
        <Border Grid.Row="2" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8" Padding="16,12" Margin="24,0,24,12"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                    <TextBlock Text="Current" FontSize="11"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               HorizontalAlignment="Center" />
                    <TextBlock x:Name="txtCurrentVersion" Text="v0.15.0" FontSize="18" FontWeight="SemiBold"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                               HorizontalAlignment="Center" />
                </StackPanel>

                <ui:SymbolIcon Grid.Column="1" Symbol="ArrowRight24" FontSize="18" Margin="16,0"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                    <TextBlock Text="New" FontSize="11"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               HorizontalAlignment="Center" />
                    <TextBlock x:Name="txtNewVersion" Text="" FontSize="18" FontWeight="SemiBold"
                               Foreground="{DynamicResource AccentFillColorDefaultBrush}"
                               HorizontalAlignment="Center" />
                </StackPanel>
            </Grid>
        </Border>

        <!-- Changelog -->
        <Border Grid.Row="3" Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8" Padding="16,12" Margin="24,0,24,16"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}" BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="What's new" FontSize="13" FontWeight="SemiBold"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,6" />
                <RichTextBox x:Name="rtbChangelog" Grid.Row="1"
                             IsReadOnly="True" BorderThickness="0" Background="Transparent"
                             Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                             FontSize="12" Padding="0"
                             VerticalScrollBarVisibility="Auto"
                             IsDocumentEnabled="True" />
            </Grid>
        </Border>

        <!-- Progress (hidden by default) -->
        <StackPanel x:Name="progressPanel" Grid.Row="4" Margin="24,0,24,16" Visibility="Collapsed">
            <ProgressBar x:Name="progressBar" Height="6" Minimum="0" Maximum="100" Value="0" />
            <TextBlock x:Name="txtProgress" Text="" FontSize="11" Margin="0,6,0,0"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <!-- Buttons -->
        <Grid Grid.Row="5" Margin="24,0,24,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="txtAssetSize" Grid.Column="0" FontSize="11" VerticalAlignment="Center"
                       Foreground="{DynamicResource TextFillColorTertiaryBrush}" />

            <ui:Button x:Name="btnLater" Grid.Column="2" Content="Later"
                       Appearance="Secondary" Height="34" Padding="20,0"
                       Margin="0,0,8,0" Click="BtnLater_Click" />
            <ui:Button x:Name="btnUpdate" Grid.Column="3" Content="Download &amp; Update"
                       Appearance="Primary" Height="34" Padding="20,0"
                       Click="BtnUpdate_Click" />
        </Grid>
    </Grid>
</ui:FluentWindow>
