<ui:FluentWindow x:Class="MergeMansionWikiTools.Views.FolderUpdateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Update Default Folder" Height="220" Width="460"
        WindowStartupLocation="CenterOwner"
        ExtendsContentIntoTitleBar="True" WindowBackdropType="Mica"
        ResizeMode="NoResize">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Update Default Folder" Height="36" />

        <Grid Grid.Row="1" Margin="24,8,24,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Question -->
            <TextBlock Grid.Row="0" Text="Set this folder as the default output location?"
                       FontSize="14" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                       TextWrapping="Wrap" Margin="0,0,0,8" />

            <!-- Path display -->
            <TextBlock x:Name="txtFolderPath" Grid.Row="1"
                       FontSize="12" Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       TextWrapping="Wrap" TextTrimming="CharacterEllipsis" MaxHeight="36"
                       Margin="0,0,0,12" />

            <!-- Don't ask again -->
            <CheckBox x:Name="chkDontAsk" Grid.Row="2"
                      Content="Don't ask again"
                      VerticalAlignment="Bottom" Margin="0,0,0,12" />

            <!-- Buttons -->
            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <ui:Button Grid.Column="1" Content="No"
                           Appearance="Secondary" Click="BtnNo_Click"
                           Margin="0,0,8,0" Width="90" Height="36" />
                <ui:Button Grid.Column="2" Content="Yes"
                           Appearance="Primary" Click="BtnYes_Click"
                           Width="90" Height="36" />
            </Grid>
        </Grid>
    </Grid>
</ui:FluentWindow>
