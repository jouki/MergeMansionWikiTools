<ui:FluentWindow x:Class="MergeMansionWikiTools.Views.MergeChainsDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Merge Chains" Width="560" MinHeight="380"
    WindowStartupLocation="CenterOwner"
    ExtendsContentIntoTitleBar="True" WindowBackdropType="Mica"
    ResizeMode="CanResizeWithGrip">

    <ui:FluentWindow.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="Merge Chains" Height="36" />

        <!-- Info bar -->
        <ui:InfoBar x:Name="infoBar" Grid.Row="1" IsOpen="True" IsClosable="False"
                    Severity="Informational" Margin="24,10,24,12"
                    Title="Chain Merge"
                    Message="Select the primary chain (radio). Alias chains will be mapped to the primary chain's name on the wiki. Level collisions between merged chains won't show warnings." />

        <!-- Scrollable chain list -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="24,0,14,0">
            <StackPanel Margin="0,0,10,0">
                <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                    <TextBlock Text="Selected Chains" FontWeight="SemiBold" FontSize="14"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                    <TextBlock Text=" — select primary (radio)"
                               FontSize="12" VerticalAlignment="Center"
                               Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                </StackPanel>

                <ItemsControl x:Name="icChains">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border CornerRadius="4" Padding="12,8" Margin="0,0,0,4">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="{DynamicResource CardBackgroundFillColorDefaultBrush}" />
                                        <Setter Property="BorderThickness" Value="0" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsExistingChain}" Value="True">
                                                <Setter Property="BorderBrush" Value="{DynamicResource AccentFillColorDefaultBrush}" />
                                                <Setter Property="BorderThickness" Value="1.5" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <RadioButton Grid.Column="0" GroupName="Primary"
                                                 VerticalAlignment="Center" Margin="0,0,0,0"
                                                 MinWidth="0" Padding="0"
                                                 IsChecked="{Binding IsPrimary, Mode=TwoWay}"
                                                 Visibility="{Binding ShowRadio, Converter={StaticResource BoolToVis}}"
                                                 Click="PrimaryRadio_Click"
                                                 ToolTip="Set as primary chain"
                                                 ToolTipService.InitialShowDelay="0" />

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="{Binding DisplayName}"
                                                       FontWeight="SemiBold" FontSize="13"
                                                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                                            <Border Background="{DynamicResource SystemFillColorCautionBackgroundBrush}"
                                                    CornerRadius="3" Padding="6,1" Margin="8,0,0,0"
                                                    VerticalAlignment="Center"
                                                    Visibility="{Binding IsExistingChain, Converter={StaticResource BoolToVis}}"
                                                    ToolTip="This chain already exists on the wiki with this name"
                                                    ToolTipService.InitialShowDelay="0">
                                                <TextBlock Text="Existing" FontSize="10" FontWeight="SemiBold"
                                                           Foreground="{DynamicResource SystemFillColorCautionBrush}" />
                                            </Border>
                                        </StackPanel>
                                        <TextBlock Text="{Binding ConfigKey}"
                                                   FontSize="11"
                                                   Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" Orientation="Horizontal"
                                                   VerticalAlignment="Center">
                                        <Border Background="{DynamicResource AccentFillColorDefaultBrush}"
                                                CornerRadius="3" Padding="6,1" Margin="0,0,8,0"
                                                Visibility="{Binding IsMainChain, Converter={StaticResource BoolToVis}}"
                                                ToolTip="This chain has no isAlias flag on the wiki — it is the existing primary chain"
                                                ToolTipService.InitialShowDelay="0">
                                            <TextBlock Text="Primary" FontSize="10" FontWeight="SemiBold"
                                                       Foreground="{DynamicResource TextOnAccentFillColorPrimaryBrush}" />
                                        </Border>
                                        <TextBlock Text="{Binding ItemCount, StringFormat='{}{0} items'}"
                                                   FontSize="11"
                                                   Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>

        <!-- Fixed bottom: name + buttons -->
        <StackPanel Grid.Row="3" Margin="24,12,24,20">
            <Border Height="1" Margin="0,0,0,12"
                    Background="{DynamicResource ControlStrokeColorDefaultBrush}" />

            <TextBlock Text="Merged Chain Name" FontWeight="SemiBold" FontSize="14"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                       Margin="0,0,0,6" />
            <ui:TextBox x:Name="txtMergedName" PlaceholderText="Enter chain name..."
                        TextChanged="TxtMergedName_TextChanged"
                        PreviewKeyDown="TxtMergedName_PreviewKeyDown" />

            <!-- Autocomplete popup -->
            <Popup x:Name="suggestPopup"
                   PlacementTarget="{Binding ElementName=txtMergedName}"
                   Placement="Bottom" StaysOpen="False"
                   AllowsTransparency="True" PopupAnimation="Fade"
                   Width="{Binding ActualWidth, ElementName=txtMergedName}">
                <Border Background="{DynamicResource SolidBackgroundFillColorBaseBrush}"
                        CornerRadius="8" Margin="0,4,0,0">
                    <Border Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                            BorderBrush="{DynamicResource ControlElevationBorderBrush}"
                            BorderThickness="1" CornerRadius="6" Padding="0,4">
                        <Border.Effect>
                            <DropShadowEffect BlurRadius="12" ShadowDepth="3" Opacity="0.18"
                                              Direction="270" Color="Black" />
                        </Border.Effect>
                        <ListBox x:Name="suggestList" Background="Transparent"
                                 BorderThickness="0" MaxHeight="200"
                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                 PreviewMouseLeftButtonUp="SuggestList_MouseUp">
                            <ListBox.ItemContainerStyle>
                                <Style TargetType="ListBoxItem">
                                    <Setter Property="Padding" Value="10,6" />
                                    <Setter Property="Cursor" Value="Hand" />
                                </Style>
                            </ListBox.ItemContainerStyle>
                        </ListBox>
                    </Border>
                </Border>
            </Popup>

            <!-- Status -->
            <TextBlock x:Name="txtStatus" Margin="0,8,0,0" FontSize="12"
                       Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                       TextWrapping="Wrap" />

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <ui:Button Content="Cancel" Appearance="Secondary"
                           Margin="0,0,8,0" Click="BtnCancel_Click" />
                <ui:Button x:Name="btnMerge" Content="Merge on Wiki"
                           Appearance="Primary" IsEnabled="False"
                           Click="BtnMerge_Click" />
            </StackPanel>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
